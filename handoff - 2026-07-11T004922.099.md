# HTML App Continuity Document
**Generated**: 2026-07-10 UTC
**By**: Super Z (GLM)
**For**: Senior mobile and web app developer (three-tier release model: Free Android+ads / Paid Android+iOS / SaaS web subscription)
**Session**: 6 (Session 1 = v6.3.0 build; Session 2 = v6.3.1 bug fixes; Session 3 = Phase 0 polish; Session 4 = Phase 1 Fill Mode; Session 5 = Phase 2 Store Packaging; Session 6 = native plugin wiring — haptics, app shortcuts, splash screen, status bar)

---

## A. Executive Summary
- **Project Goal**: Templateify Pro — an offline-first, single-file HTML PWA for creating, organizing, filling in, and exporting reusable text templates. Session 6 wires the remaining native Capacitor plugins that justify the App Store submission (Guideline 4.2 mitigation) and add mobile polish.
- **Current Status**: Working. Session 6 delivered as one atomic unit. All 4 sub-tasks (S6.1 haptics, S6.2 wiring, S6.3 app shortcuts, S6.4 native chrome) implemented and verified via Playwright on both `file://` and `http://` + simulated native (Capacitor stub with Haptics + App + StatusBar + SplashScreen plugins). **25/25 automated checks pass.**
- **Last Action Taken**: Implemented Session 6 surgically — `haptic()` helper (native Haptics plugin + web `navigator.vibrate` fallback), wired haptics into `copyFinalText` (light on success), `removeTemplate` (medium on delete), `switchToFillMode`/`switchToEditMode` (light on mode switch). Added `setupAppShortcuts()` listener for `appShortcutItem` events (routes `new_template` → `createNew()`, `search` → `toggleSearch()`, `fill_recent` → `loadTemplateUI(first)`). Added `setupNativeChrome()` for status bar (matches brand color, re-applies on theme toggle) + splash screen (hide after boot). Updated `capacitor.config.json` with `App.shortcuts` (3 shortcuts), `SplashScreen` config, `StatusBar` config. Updated `capacitor/package.json` to add `@capacitor/splash-screen` + `@capacitor/status-bar`. Regenerated `manifest.json` (v7.1.0) and `sw.js` (`CACHE_NAME = templateify-v7.1.0`). Authored this `handoff.md`.
- **Next Required Action**: User should (1) replace local `index.html`, `manifest.json`, `sw.js`, `/capacitor/` with v7.1.0 copies, (2) run `npm install` in `/capacitor/` to install the two new plugins, (3) `npx cap sync` to push changes to native projects, (4) test on a real Android device (haptics + app shortcuts + splash + status bar), (5) generate store screenshots, (6) await the user's go-ahead to begin Phase 3 (Cloud/SaaS).
- **Release Tier**: All three. The web app is unchanged in behavior on web/file:// (haptics no-op, shortcuts no-op, splash/status bar no-op). On native (Capacitor), all four features activate. This is the **final pre-submission** web-side release — the next step is native build testing + store submission.
- **Architecture**: Single-file `index.html` (PWA) — UNCHANGED. Companion artifacts: `manifest.json`, `sw.js`, `handoff.md`, `/capacitor/` (native wrapper project, now with 11 plugin deps), `/store/` (policy + ASO docs), `/icons/` (user-supplied).
- **Session Count**: 6
- **Original AI**: Unknown (Session 1 — built v6.3.0 feature patch)
- **Last AI**: Super Z (GLM) — Session 6 (native plugin wiring: haptics, app shortcuts, splash screen, status bar)

---

## B. App Architecture Breakdown

### Region Inventory

The app shell is unchanged from v7.0.0. **Session 6 added no new DOM regions** — all changes are JS functions + Capacitor config.

### CSS System Map

**UNCHANGED from v7.0.0.** No new CSS rules in Session 6.

### JS Module Map

**Session 6 added the following JS modules** (in the `<script>` block):

| Module | Type | Created In | Used By | Last Known State |
|--------|------|-----------|---------|------------------|
| `HAPTIC_STYLES` (NEW S6.1) | const object | line ~1580 | `haptic()` | 4 styles: light (8ms), medium (15ms), heavy (25ms), selection (5ms) |
| `haptic(style)` (NEW S6.1) | async function | line ~1586 | `switchToFillMode()`, `switchToEditMode()`, `copyFinalText()`, `removeTemplate()` | On native: calls `Haptics.impact({style: 1\|2\|3})` or `Haptics.selection()`. On web: falls back to `navigator.vibrate(ms)` (Chrome Android only). Silently fails on Safari/Firefox. |
| `setupAppShortcuts()` (NEW S6.3) | function | line ~1614 | Boot (DOMContentLoaded) | On native: registers `App.addListener('appShortcutItem', ...)` listener. On web: no-op. |
| `handleAppShortcut(shortcutId)` (NEW S6.3) | function | line ~1631 | `setupAppShortcuts()` listener callback | Routes `new_template` → `createNew()`, `search` → `toggleSearch()`, `fill_recent` → `loadTemplateUI(state.templates[0].id)`. 300ms delay so app finishes booting first. |
| `setupNativeChrome()` (NEW S6.4) | async function | line ~1659 | Boot (DOMContentLoaded), `toggleTheme()` | On native: configures status bar (style + background color matching theme) + hides splash screen. On web: no-op. |

**Existing modules MODIFIED in Session 6:**
- `APP_VERSION` → `'7.1.0 Pro'` (bumped from `'7.0.0 Pro'`)
- `toggleTheme()` → now calls `setupNativeChrome()` after theme change (re-applies status bar color on native; no-op on web)
- `switchToFillMode()` → now calls `haptic('light')` at the end
- `switchToEditMode()` → now calls `haptic('light')` at the end
- `copyFinalText()` → now calls `haptic('light')` inside the `.then()` callback (only on successful copy)
- `removeTemplate()` → now calls `haptic('medium')` after delete (before the undo toast)
- Boot sequence (DOMContentLoaded) → now calls `setupAppShortcuts()` + `setupNativeChrome()` in addition to existing `loadProState()` + `showAdBanner()`

### Storage Map

**UNCHANGED from v7.0.0.** No new settings keys, no DB schema changes. Session 6 is pure runtime behavior (haptics, shortcuts, chrome) — nothing persisted.

### Data Flow Diagram

```
Boot sequence (v7.1.0 additions)
  └── DOMContentLoaded
      ├── (existing: initDatabase, loadStateFromStorage, loadTheme, etc.)
      ├── Phase 1.5: loadPhase1Settings() (auto-save, default export, remember default)
      ├── Phase 2.1: loadProState() + showAdBanner() (after 800ms + 4000ms delays)
      └── Session 6:
          ├── setupAppShortcuts()
          │   └── If isNative && App plugin available:
          │       └── App.addListener('appShortcutItem', (data) => handleAppShortcut(data.id))
          └── setupNativeChrome()
              └── If isNative:
                  ├── StatusBar.setStyle({ style: 'LIGHT'|'DARK' })  // based on state.darkMode
                  ├── StatusBar.setBackgroundColor({ color: '#4f46e5'|'#0f172a' })  // brand or slate
                  └── SplashScreen.hide()

User long-presses app icon → taps "New Template" shortcut (NATIVE)
  └── OS launches app + fires 'appShortcutItem' event with { id: 'new_template' }
  └── setupAppShortcuts listener → handleAppShortcut('new_template')
      └── setTimeout(() => createNew(), 300)
          └── (if current template has unsaved tokens: customConfirm dialog)
          └── state.currentTemplateId = null, clear state arrays
          └── switchToEditMode() (new template → Edit Mode)
              └── haptic('light')  // S6.2

User taps Copy in Fill Mode (NATIVE)
  └── copyFinalText()
      └── renderFinalText(template, values) → plainText + richHtml
      └── copyStructuredToClipboard(plainText, richHtml).then(ok => {
          ├── showToast('Copied to clipboard!')
          └── if (ok) haptic('light')  // S6.2 — native: Haptics.impact({style: 1})
      })

User deletes a template (NATIVE)
  └── removeTemplate(id, e)
      └── customConfirm("Delete this template?")
      └── snapshotForUndo(template)
      └── state.templates = filter(...)
      └── persistState()
      └── renderTemplateList()
      └── haptic('medium')  // S6.2 — native: Haptics.impact({style: 2})
      └── showToastWithAction('Deleted "..."' , 'Undo', ...)

User toggles theme (NATIVE)
  └── toggleTheme()
      └── enableLightMode() / enableDarkMode()
      └── DataLayer.saveSetting('theme', ...)
      └── if (isNative) setupNativeChrome()  // S6.4 — re-applies status bar color
          └── StatusBar.setStyle({ style: newIsDark ? 'DARK' : 'LIGHT' })
          └── StatusBar.setBackgroundColor({ color: newIsDark ? '#0f172a' : '#4f46e5' })

Service Worker lifecycle (http(s) only — skipped on file:// AND inside Capacitor)
  └── UNCHANGED from v7.0.0. CACHE_NAME = 'templateify-v7.1.0', 18 precache URLs.
```

### CDN Dependency Map

**UNCHANGED from v7.0.0.** No new CDN libraries. All 9 CDN URLs in `<head>` are still listed in `sw.js` `PRECACHE_URLS`.

### Capacitor Plugin Dependency Map (NEW in Session 6)

| Plugin | Version | Purpose | Web behavior | Native behavior |
|--------|---------|---------|--------------|-----------------|
| `@capacitor/core` | ^6.0.0 | Runtime | `window.Capacitor` (no-op) | Native bridge |
| `@capacitor/android` | ^6.0.0 | Android platform | — | Android project |
| `@capacitor/ios` | ^6.0.0 | iOS platform | — | iOS project |
| `@capacitor/app` | ^6.0.0 | App lifecycle + shortcuts | No-op | **S6.3**: `appShortcutItem` listener for 3 shortcuts |
| `@capacitor/haptics` | ^6.0.0 | Haptic feedback | No-op (web fallback to `navigator.vibrate`) | **S6.1/S6.2**: `impact({style})` + `selection()` |
| `@capacitor/share` | ^6.0.0 | Native share sheet | `navigator.share` (Capacitor intercepts) | Native share sheet (already wired via `shareFinalText()`) |
| `@capacitor/preferences` | ^6.0.0 | Native key-value store | No-op | Optional (for Pro license caching — not yet wired, `IndexedDB` is current source of truth) |
| **`@capacitor/splash-screen`** (NEW S6.5) | ^6.0.0 | Splash screen | No-op | **S6.4**: `SplashScreen.hide()` after boot; config in `capacitor.config.json` |
| **`@capacitor/status-bar`** (NEW S6.5) | ^6.0.0 | Status bar styling | No-op | **S6.4**: `setStyle()` + `setBackgroundColor()` on boot + theme toggle; config in `capacitor.config.json` |
| `@capacitor-community/admob` | ^6.0.0 | AdMob banner ads | No-op | Phase 2.2: `showBanner()` / `hideBanner()` |
| `@capacitor-community/in-app-purchases` | ^6.0.0 | IAP Pro unlock | No-op | Phase 2.1: `checkPurchase()` / `purchase()` / `restorePurchases()` |

---

## C. Error Log & Diagnostics

### Last Known Error (Session 6 — two test artifacts, no real bugs)

**Region/Module**: Test harness only

**Error Type**: Two verification script issues (not app bugs)

**Full Console Output**:
1. `Blocked call to navigator.vibrate because user hasn't tapped on the frame or any embedded frame yet` — Chromium security feature. The test called `switchToEditMode()` directly without a prior user tap. In real usage, haptics fire after a button tap (which satisfies the gesture requirement). Filtered out of the test's error check.
2. `App shortcut "new_template" → createNew()` initially failed because `createNew()` shows a `customConfirm` dialog when the current template has unsaved tokens. The test didn't auto-dismiss the dialog. Fixed by stubbing `customConfirm` to return `true` during the shortcut test.

**Fix Status**: Both test-harness issues fixed. App itself has no errors. **25/25 checks pass.**

**Attempted By**: Super Z (GLM) — Session 6

### Error History (Chronological)

| # | Error | Region | Attempted Fix | Result | AI Session |
|---|-------|--------|---------------|--------|------------|
| 1–14 | (carried over from Sessions 2–4) | various | see prior handoffs | Fixed/Accepted | Sessions 2–4 |
| 15 | `#adBanner` never shown — `hidden` Tailwind class overrode `.active` | `showAdBanner()` | Updated both functions to toggle `hidden` + `active` | Fixed | Session 5 |
| **16** | **Test: `navigator.vibrate` blocked without user gesture** | **Test harness (not app)** | **Filtered out of error check — expected Chromium security behavior** | **Accepted (test artifact)** | **Session 6** |
| **17** | **Test: `new_template` shortcut didn't fire `createNew()` because `customConfirm` dialog blocked** | **Test harness (not app)** | **Stubbed `customConfirm` to return `true` during shortcut test** | **Fixed (test fix)** | **Session 6** |

---

## D. Recommended Next Prompt (Copy-Paste Ready)

Paste this into your next AI session:

---

I have a single-file HTML PWA called "Templateify Pro" (v7.1.0 Pro). It's an offline-first template management app built as one self-contained `index.html` file with companion `manifest.json`, `sw.js`, a `/capacitor/` wrapper project (with 11 plugin deps including haptics, app shortcuts, splash screen, status bar), and a `/store/` directory with policy docs + ASO copy.

**History**:
- Session 1: built v6.3.0 (`.tmpf` export/import, unified action toolbar).
- Session 2: fixed 5 bugs (theme toggle, search TDZ, Export button, CORS manifest, SW registration). Delivered v6.3.1.
- Session 3: delivered Phase 0 polish (rename map, empty states, onboarding + samples, undo delete, mobile bottom bar, settings stub, theme toggle relocation). Delivered v6.4.0.
- Session 4: delivered Phase 1 Fill Mode (form-style Use view, sticky Copy bar, remember-last-values, expanded Settings, DB v9 migration). Delivered v6.5.0. 32/32 checks pass.
- Session 5: delivered Phase 2 Store Packaging (feature gating, AdMob/IAP layer, Capacitor project, policy docs, ASO copy). Delivered v7.0.0. 34/34 checks pass.
- Session 6 (just completed): delivered native plugin wiring — `haptic()` helper (native Haptics + web vibrate fallback), wired into copy/delete/mode-switch, `setupAppShortcuts()` (3 shortcuts: New/Search/Fill Recent), `setupNativeChrome()` (status bar + splash screen), updated `capacitor.config.json` + `capacitor/package.json`. Delivered v7.1.0. **25/25 checks pass.**

**Approved roadmap**:
- ✅ Phase 0 — Polish (DONE in Session 3)
- ✅ Phase 1 — Fill Mode (DONE in Session 4)
- ✅ Phase 2 — Store Packaging (DONE in Session 5)
- ✅ Session 6 — Native plugin wiring (DONE in Session 6)
- ⏳ Phase 3 (NEXT): Cloud/SaaS — Firebase Auth + Firestore sync (IndexedDB stays source of truth offline; cloud sync for Paid/SaaS), Stripe checkout on web, plan-based feature gating, account/data-deletion page, ToS + Privacy Policy pages.
- Phase 4 — Growth: mail-merge, share links/QR, AI template extractor, team folders, template marketplace.

**What I want you to do next** (Session 7 = Phase 3 start):

Implement Phase 3 — Cloud/SaaS sync. Per the approved plan in §5 of the product brief:

1. **Firebase Auth** — email + Google sign-in. Load Firebase SDK lazily (dynamic import / injected script) ONLY after the user signs in, so free/offline users and file:// pay zero performance cost. Add SDK URLs to `sw.js` PRECACHE_URLS when enabled; bump CACHE_NAME.

2. **Firestore per-user data** — `users/{uid}/templates/{id}` and `users/{uid}/folders/{id}`. Security rules: read/write only when `request.auth.uid == uid`.

3. **Sync engine** — IndexedDB remains source of truth offline. Bump DB to v10 adding `dirty` (boolean) and `deletedAt` (tombstone) fields with a migration. Sync = push dirty docs, pull docs changed since `lastSyncAt`, conflict resolution = last-write-wins on `updatedAt` (v1; surface conflicts in UI later).

4. **Stripe checkout on web** — for the SaaS subscription tier ($2.99/mo or ₱149/mo, ~2 months free annual). Web users get a "Subscribe to Cloud" option in Settings that opens Stripe Checkout. Store the subscription status in Firestore `users/{uid}/subscription`.

5. **Plan-based feature gating** — extend the existing `state.isPro` to `state.plan` ('free' | 'pro' | 'saas'). Pro = one-time IAP (existing). SaaS = monthly sub (new). SaaS includes everything in Pro + cloud sync + share-a-template links + version history.

6. **Account/data-deletion page** — required by GDPR + Play Store + App Store. A simple web page at `/account` where users can delete their account + all cloud data.

7. **ToS + Privacy Policy updates** — update `/store/privacy-policy.html` + `/store/terms-of-service.html` to cover cloud sync, Firebase, Stripe, and data deletion.

**Critical constraints** (do NOT violate):
- Single-file architecture: all HTML/CSS/JS stays in `index.html`. No build step.
- Firebase SDK MUST be loaded lazily — free/offline/file:// users must never pay the performance cost. Use dynamic `import()` or an injected `<script>` that only loads after sign-in.
- IndexedDB remains the source of truth. Cloud sync is additive — if it fails, the app still works fully offline.
- DB migration to v10 must be backward-compatible.
- The `file://` protocol must keep working. Do NOT remove the `isFileProtocol` guards.
- Capacitor plugin calls MUST be gated behind `isNative === true`.
- Generate/update `handoff.md` per skill §13.6 after every change. Increment the Session Count to 7.
- Ship as one atomic delivery: `index.html` + `manifest.json` (bump to v8.0.0) + `sw.js` (`CACHE_NAME = templateify-v8.0.0`) + `handoff.md` + updated `/capacitor/` + `/store/` files.

The current files are at:
- `/home/z/my-project/download/index.html`
- `/home/z/my-project/download/manifest.json`
- `/home/z/my-project/download/sw.js`
- `/home/z/my-project/download/handoff.md`
- `/home/z/my-project/download/capacitor/` (package.json, capacitor.config.json, README.md)
- `/home/z/my-project/download/store/` (privacy-policy.html, terms-of-service.html, play-data-safety.md, aso-copy.md, store-assets-checklist.md)

Please start by reading all the files + the handoff, then implement Phase 3 surgically (minimal diffs), keeping ALL hard constraints, regenerating `manifest.json`/`sw.js` when required, and updating `handoff.md` (increment Session Count to 7).

---

## E. Environment State Snapshot

### Browser / Device Status (Last Check)
- **Browser**: Chromium (headless, via Playwright) — tested with and without Capacitor stub (Haptics + App + StatusBar + SplashScreen plugins stubbed)
- **OS**: Linux (verification) / Windows (user) / Android (target, untested) / iOS (target, untested)
- **Screen**: Desktop 1280x800, mobile 360x640 (both tested)
- **Service Worker**: `templateify-v7.1.0`. Registered only on `http(s)://`. Inside Capacitor: SW is a no-op.

### PWA Alignment Verification

- **manifest.json**: Aligned ✓ — `version: "7.1.0"`, all icon paths use `?v=7.1.0`.
- **sw.js**: Aligned ✓ — `CACHE_NAME: 'templateify-v7.1.0'`, 18 PRECACHE_URLS (same as v7.0.0 — no new CDN libs).
- **capacitor.config.json**: Aligned ✓ — includes `App.shortcuts` (3), `SplashScreen` config, `StatusBar` config.
- **capacitor/package.json**: Aligned ✓ — 11 plugin deps (added `@capacitor/splash-screen` + `@capacitor/status-bar`).
- **PWA Alignment Verifier result**: **25/25 automated checks pass.**

#### Verification Test Matrix (Playwright headless Chromium, run 2026-07-10)

| Check | `file://` | `http://` | Native stub |
|-------|-----------|-----------|-------------|
| Boot toast v7.1.0 | PASS | PASS | PASS |
| haptic() helper exists | PASS | PASS | (n/a) |
| haptic() no-op on web (no error) | (n/a) | PASS | (n/a) |
| HAPTIC_STYLES has 4 styles | (n/a) | PASS | (n/a) |
| setupAppShortcuts() exists | (n/a) | PASS | (n/a) |
| setupNativeChrome() exists | (n/a) | PASS | (n/a) |
| handleAppShortcut() exists | (n/a) | PASS | (n/a) |
| toggleTheme() still works | (n/a) | PASS | (n/a) |
| switchToEditMode() fires haptic (web vibrate) | (n/a) | PASS | (n/a) |
| switchToFillMode() fires haptic | (n/a) | PASS | (n/a) |
| copyFinalText() fires haptic on success | (n/a) | PASS | (n/a) |
| No real pageerror on http:// | (n/a) | PASS | (n/a) |
| Fill Mode active on file:// (no regression) | PASS | (n/a) | (n/a) |
| No CORS/manifest errors on file:// | PASS | (n/a) | (n/a) |
| isNative = true with Capacitor stub | (n/a) | (n/a) | PASS |
| App shortcut listener registered on native | (n/a) | (n/a) | PASS |
| App shortcut "new_template" → createNew() | (n/a) | (n/a) | PASS |
| App shortcut "search" → toggleSearch() | (n/a) | (n/a) | PASS |
| App shortcut "fill_recent" → loadTemplateUI(first) | (n/a) | (n/a) | PASS |
| Status bar configured on native boot | (n/a) | (n/a) | PASS (style: LIGHT, color: #4f46e5) |
| Splash screen hidden on native boot | (n/a) | (n/a) | PASS |
| switchToEditMode() fires native Haptics.impact | (n/a) | (n/a) | PASS (impact:1) |
| toggleTheme() re-configures status bar on native | (n/a) | (n/a) | PASS |

### CDN Dependencies — UNCHANGED from v7.0.0

### File System State

```
/home/z/my-project/download/
├── index.html              (~275KB, ~4,160 lines) — v7.1.0 Session 6 native wiring
├── manifest.json           (v7.1.0)
├── sw.js                   (CACHE_NAME = templateify-v7.1.0, 18 precache URLs)
├── handoff.md              (this file)
├── capacitor/              (updated in S6.5)
│   ├── package.json        (11 deps — added @capacitor/splash-screen + @capacitor/status-bar)
│   ├── capacitor.config.json  (added App.shortcuts + SplashScreen + StatusBar config)
│   └── README.md           (unchanged from v7.0.0 — 200-line setup guide)
├── store/                  (unchanged from v7.0.0)
│   ├── privacy-policy.html
│   ├── terms-of-service.html
│   ├── play-data-safety.md
│   ├── aso-copy.md
│   └── store-assets-checklist.md
└── icons/                  (NOT BUNDLED — user must supply)
```

### Storage State (Last Check)
- **localStorage**: `installBannerDismissed`
- **IndexedDB**: `TypeFillDB` v9. Settings: `theme`, `templatesHidden`, `onboarded`, `samplesSeeded`, `defaultExportFormat`, `autoSave`, `rememberFillValuesDefault`, `isPro`. (No new settings in Session 6.)
- **Cache API**: `templateify-v7.1.0` with 18 entries.

### Known Warnings (Non-Fatal)
- `cdn.tailwindcss.com should not be used in production` — accepted tradeoff.
- `[Intervention] Ignored attempt to cancel a touchend event` — benign Chrome intervention.
- On `file://`: manifest/icon `<link>` tags suppressed (by design).
- Playwright spurious "Failed to update a ServiceWorker... invalid state" during test teardown — test artifact.
- **NEW in Session 6**: On web, `navigator.vibrate` may log a warning if called without a user gesture (Chromium security feature). In real usage, haptics fire after a button tap, which satisfies the gesture requirement. The warning is harmless.

---

## F. Decision Log

| # | Decision | Rationale | Alternatives Rejected |
|---|----------|-----------|----------------------|
| 1–53 | (carried over from Sessions 2–4) | see prior handoffs | — |
| 54–80 | (carried over from Session 5) | see Session 5 handoff | — |
| **81** | **S6.1 — `haptic()` uses native Haptics plugin on native, `navigator.vibrate` on web** | **The Haptics plugin provides proper iOS Taptic Engine + Android vibration patterns. `navigator.vibrate` is the web fallback (Chrome Android only — Safari/Firefox ignore it silently). The dual-path ensures haptics work everywhere they can.** | **(a) Native-only (rejected: web users get no feedback). (b) `navigator.vibrate` only (rejected: misses iOS Taptic Engine, which is the premium haptic experience). (c) Audio feedback (rejected: annoying in quiet environments)** |
| **82** | **S6.1 — HAPTIC_STYLES maps to numeric impact styles (1=LIGHT, 2=MEDIUM, 3=HEAVY)** | **The `@capacitor/haptics` plugin's `impact()` function takes a numeric `style` parameter (1/2/3), not a string. The HAPTIC_STYLES const documents the mapping but the actual call uses the number. Selection is a separate API (`Haptics.selection()`) with no style parameter.** | **Using string constants directly (rejected: plugin expects numbers)** |
| **83** | **S6.1 — `haptic()` silently fails (catch + no re-throw)** | **Haptics are a polish feature — they must NEVER block UX or show an error. If the plugin is missing, the device doesn't support haptics, or the call fails for any reason, the app continues normally. The catch block has a commented-out `console.warn` for debugging.** | **(a) Throw on error (rejected: would break copy/delete if haptics fail). (b) Show a toast (rejected: annoying + draws attention to a non-issue)** |
| **84** | **S6.2 — Light haptic on Copy success, medium on Delete, light on mode switch** | **Copy is the primary action — light haptic confirms success without being aggressive. Delete is destructive — medium haptic signals "that was a real action". Mode switches are navigation — light haptic provides tactile feedback without being noisy.** | **(a) Heavy on delete (rejected: too aggressive for a recoverable action — undo exists). (b) No haptic on mode switch (rejected: misses the tactile confirmation). (c) Selection haptic on mode switch (rejected: too subtle)** |
| **85** | **S6.2 — Haptic fires AFTER the action completes, not before** | **Firing before would feel like the action hasn't happened yet. Firing after confirms the action succeeded. For copy: fires inside `.then()` (after clipboard write). For delete: fires after `persistState()` + `renderTemplateList()`. For mode switch: fires at the end of the function.** | **(a) Fire before (rejected: feels premature). (b) Fire during (rejected: hard to time correctly)** |
| **86** | **S6.3 — 3 app shortcuts: New Template, Search, Fill Recent** | **These are the 3 most common entry points. "New Template" for quick capture. "Search" for finding an existing template. "Fill Recent" for the most-used template (1-tap fill). 3 is the Android limit for app shortcuts; iOS allows 4 but 3 keeps it consistent.** | **(a) 4 shortcuts (rejected: Android limit is 3). (b) Only "New Template" (rejected: misses the search + fill-recent use cases). (c) "Settings" as a shortcut (rejected: low frequency)** |
| **87** | **S6.3 — `handleAppShortcut` uses a 300ms delay before routing** | **The `appShortcutItem` event fires very early in the boot sequence — before `loadStateFromStorage()` completes. The delay ensures `state.templates` is populated before `fill_recent` tries to load one. 300ms is enough for boot to finish but short enough to feel instant.** | **(a) No delay (rejected: `state.templates` may be empty → "No templates yet" toast). (b) Wait for a specific boot-complete event (rejected: over-engineered for a 300ms delay)** |
| **88** | **S6.3 — App shortcut config lives in `capacitor.config.json` (declarative), not in JS** | **Capacitor 6's `@capacitor/app` plugin reads shortcuts from the config file at build time. The JS side only listens for events. This is the standard Capacitor pattern — declarative config + imperative event handling.** | **(a) Register shortcuts in JS (rejected: not supported by the plugin). (b) Hardcode in native AndroidManifest.xml / Info.plist (rejected: bypasses Capacitor's abstraction)** |
| **89** | **S6.4 — Status bar color matches the brand color (indigo-600) on light, slate-900 on dark** | **The status bar should feel like part of the app, not a separate OS element. Matching the header color (indigo-600 in light, slate-900 in dark) creates a seamless look. The `overlaysWebView: false` config ensures the status bar doesn't overlap content.** | **(a) Default OS color (rejected: looks like a web wrapper). (b) Always indigo-600 (rejected: clashes with dark mode). (c) Always slate-900 (rejected: clashes with light mode)** |
| **90** | **S6.4 — `toggleTheme()` calls `setupNativeChrome()` after theme change** | **When the user toggles dark/light mode, the status bar color must update to match. Modifying `toggleTheme()` directly (rather than wrapping it) is cleaner — the wrap approach failed in S6.1 because `toggleTheme` is defined after the wrap runs at parse time.** | **(a) Wrap `toggleTheme` at parse time (rejected: `toggleTheme` is undefined when the wrap runs). (b) Listen for a 'themeChanged' event (rejected: over-engineered). (c) Don't update status bar on theme toggle (rejected: status bar stays wrong color in dark mode)** |
| **91** | **S6.4 — Splash screen hidden via `SplashScreen.hide()` after boot** | **The `@capacitor/splash-screen` plugin shows the splash on launch automatically (per `capacitor.config.json`). `hide()` is called after boot to dismiss it. The `launchAutoHide: true` config is a backup — if `hide()` never fires, the splash auto-hides after `launchShowDuration` (1500ms).** | **(a) Only use `launchAutoHide` (rejected: less control — the splash might hide before boot completes). (b) Never hide programmatically (rejected: splash stays visible too long if `launchShowDuration` is high)** |
| **92** | **S6.5 — Added `@capacitor/splash-screen` + `@capacitor/status-bar` to `capacitor/package.json`** | **These are official Capacitor plugins (maintained by the Capacitor team). They're stable, well-documented, and compatible with Capacitor 6. Required for the App Store 4.2 mitigation (native splash + status bar = "not a thin web wrapper").** | **(a) Use a custom native splash (rejected: more work, less portable). (b) Skip splash + status bar (rejected: App Store 4.2 risk)** |
| **93** | **S6 — Bumped `APP_VERSION` to `'7.1.0 Pro'`** | **Session 6 adds native plugin wiring — a minor feature release. 7.0 → 7.1 reflects the incremental addition (not a major milestone like 7.0 was).** | **(a) v7.0.1 (rejected: patch version implies bug fix, not feature). (b) v8.0.0 (rejected: not a major milestone)** |
| **94** | **S6 — Bumped `CACHE_NAME` to `templateify-v7.1.0` + manifest `version` to `7.1.0`** | **Per skill §3.7.2: bump `CACHE_NAME` on every regeneration. Both track v7.1.0.** | **Keeping v7.0.0 (rejected: users would get stale cached v7.0.0)** |

---

## G. Quick Reference for Next AI

If this is a new AI session reading this document:

- **The app is at**: `/home/z/my-project/download/index.html` (companion: `manifest.json`, `sw.js`, this `handoff.md`, `/capacitor/`, `/store/`, `/icons/`)
- **The error is in Region/Module**: None — the app is working. All Session 6 sub-tasks are implemented and verified. **25/25 automated checks pass.**
- **The variable to inspect first is**: `state` — it holds `templates`, `folders`, `currentTemplateId`, `currentVariables/Choices/Masked/Hashtags`, `currentTemplateLocked`, `darkMode`, `isPro`, `templatesHidden`, `recentlyDeleted`, `fillMode`, `fillValues`. Also check `isNative` (global const) to see if running in Capacitor.
- **The most likely fix direction is**: N/A — the app is working. For Phase 3 (Cloud/SaaS), start by reading Section B (Capacitor Plugin Dependency Map) + the Data Flow Diagram + `/store/privacy-policy.html` (which will need updating for cloud sync).
- **DO NOT start from scratch.** Read this entire `handoff.md` BEFORE examining the app source.
- **Critical constraints** (do NOT violate):
  - Single-file architecture: all HTML/CSS/JS stays in `index.html`. No build step.
  - CDN libraries must be pinned to specific versions (never `@latest`).
  - The `file://` protocol must keep working. Do NOT remove the `isFileProtocol` guards.
  - DB migrations must be backward-compatible. DB is currently at v9.
  - Capacitor plugin calls MUST be gated behind `isNative === true`.
  - **NEW in Session 6**: Do NOT remove `haptic()`, `HAPTIC_STYLES`, `setupAppShortcuts()`, `handleAppShortcut()`, `setupNativeChrome()`, or the haptic calls in `switchToFillMode()`/`switchToEditMode()`/`copyFinalText()`/`removeTemplate()` — they back the native polish features.
  - **NEW in Session 6**: Do NOT remove the `App.shortcuts`, `SplashScreen`, or `StatusBar` config from `capacitor.config.json` — they're required for the native features to work.
  - **NEW in Session 6**: Do NOT remove `@capacitor/splash-screen` or `@capacitor/status-bar` from `capacitor/package.json` — they're required dependencies.
  - **NEW in Session 6**: The `toggleTheme()` function now calls `setupNativeChrome()` — do NOT remove this call or the status bar color won't update on theme toggle (native only).
